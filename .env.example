# Copy this to .env and fill in real values

# Generate SECRET_KEY with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-this-with-output-of-command-above
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

DATABASE_URL=postgresql://caseflow:your-db-password@localhost:5432/caseflow_mb
APP_DATABASE_URL=postgresql://caseflow_app:your-app-password@localhost:5432/caseflow_mb
